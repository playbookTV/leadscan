[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@8.15.0", "pnpm install --frozen-lockfile --filter @leadscout/web..."]

[phases.build]
cmds = ["cd apps/web && pnpm build"]

[start]
cmd = "npx serve -s apps/web/dist -l $PORT"