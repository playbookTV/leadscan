[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@10.22.0", "pnpm install --frozen-lockfile --filter @leadscout/api..."]

[start]
cmd = "cd apps/api && node src/index.js"